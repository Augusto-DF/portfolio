import Phaser from 'phaser'

const GamePortfolioView = () => {
  const containerStyle = {
    width: '100vw',
    height: '80vh',
  }

  const phaserConfig = {}
  //const game = Phaser(phaserConfig)

  return <div style={containerStyle}>GamePortfolioView</div>
}

export default GamePortfolioView
